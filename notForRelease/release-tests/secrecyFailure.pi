(*  param debugOutput = true.  *)

free c.
free init.

private free k.

query attacker:k.

cell s := (init,init).
cell t := (init,init).

let T =
read s as x;
read t as y;
s := k;
out (c, x);
out (c, y).



process T
