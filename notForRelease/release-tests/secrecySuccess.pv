
free c:channel.
free init:bitstring.
free k:bitstring [private].

query attacker(k).


cell s:= (init, init).
cell t:= (init, init).

let T =
read s as x:bitstring;
read t as y:bitstring;
out (c, x);
out (c, y).



process T
